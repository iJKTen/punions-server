'use strict';

module.exports.connectionManager = async (event, context) => {
  return {
    statusCode: 200
  };
};

module.exports.sendMessage = async (event, context) => {
  return {
    statusCode: 200
  };
};
